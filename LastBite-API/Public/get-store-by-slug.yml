info:
  name: Get Store By Slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/stores/:storeSlug"
  params:
    - name: storeSlug
      value: "{{storeSlug}}"
      type: path
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
