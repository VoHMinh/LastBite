info:
  name: Get Bag Detail
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/bags/:bagId"
  params:
    - name: bagId
      value: "{{bagId}}"
      type: path
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
