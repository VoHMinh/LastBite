info:
  name: List Bags By Store
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/v1/merchant/bags/store/:storeId"
  params:
    - name: storeId
      value: ""
      type: path
      required: true
    - name: page
      value: "0"
      type: query
    - name: size
      value: "20"
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5