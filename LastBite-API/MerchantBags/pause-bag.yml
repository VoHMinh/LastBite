info:
  name: Pause Bag
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/merchant/bags/:bagId/pause"
  params:
    - name: bagId
      value: "{{bagId}}"
      type: path
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
