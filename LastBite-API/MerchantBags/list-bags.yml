info:
  name: List Bags
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/merchant/bags"
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "20"
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
