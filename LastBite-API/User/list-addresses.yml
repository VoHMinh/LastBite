info:
  name: List Addresses
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/me/addresses"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
