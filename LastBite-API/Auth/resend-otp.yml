info:
  name: Resend OTP
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/resend-otp"
  body:
    type: json
    data: |-
      {
        "email": "customer@example.com"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
