info:
  name: Logout All
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/logout-all"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
