info:
  name: Logout
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/logout"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
