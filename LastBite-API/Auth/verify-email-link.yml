info:
  name: Verify Email Link
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/verify-email-link"
  params:
    - name: token
      value: paste-email-token-here
      type: query
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
