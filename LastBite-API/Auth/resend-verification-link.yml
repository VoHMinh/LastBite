info:
  name: Resend Verification Link
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/resend-verification-link"
  body:
    type: json
    data: |-
      {
        "email": "customer@example.com"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
