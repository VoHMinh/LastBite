info:
  name: Pause Store
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/store-owner/store/pause"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
