info:
  name: Get My Store
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/store-owner/store"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
