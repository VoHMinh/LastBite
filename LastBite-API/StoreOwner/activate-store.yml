info:
  name: Activate Store
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/store-owner/store/activate"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
